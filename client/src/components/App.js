//naming conventions: if file is exporting a class or react component of any type 
// (functional component or class based component) use CAPITAL letter
//else if file returns only function or series of functions use LOWER case letter
import React  from 'react';

const App = () => {
    return (
        <div>
            Hi there!
        </div>
    );
};

export default App